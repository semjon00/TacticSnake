NET functionality:

Packets should be the following:
Server:
    roomConnectionFailure(msg)
    roomConnectionSuccess(settings, field)
    ping()
    notifyTurn(snakei, absx, absy, nextsnakei)
    notifyMessage(id, msg)
    notifyLose(snakei)
    notifyWin(snakei)
    requestTurn(snakei)

Client:
    roomConnect(my_version, room)
    ping()
    madeTurn(deltax, deltay)
    message(str)
